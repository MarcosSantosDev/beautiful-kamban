import { theme } from '@/styles/stitches.config';

export const templateApp = {
  gap: theme.sizes[4],
  navbar: {
    height: '80px',
  },
  sidebar: {
    width: '300px',
  },
  boadContainer: {
    marginBottom: theme.sizes[4],
  },
};
